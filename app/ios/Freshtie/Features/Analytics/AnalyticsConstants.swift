import Foundation

/// Standardized metadata keys for consistency across events.
enum AnalyticsMetadata {
    static let personID = "person_id"
    static let sourceType = "source_type"
    static let status = "status"
    static let promptCount = "prompt_count"
}
